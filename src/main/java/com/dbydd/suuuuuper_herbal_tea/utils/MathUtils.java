package com.dbydd.suuuuuper_herbal_tea.utils;

public class MathUtils {
    public static boolean inCircle(int x, int y, double range) {
        return Math.pow(x, 2) + Math.pow(y, 2) <= range;
    }

}
